package server.server;

class ServerApplicationTests {
    // 1. Domain 에서 Entity 와 Dto 변환을 확인
    
    // 2. Service 에서 exception 로직을 확인 (변환, 저장은 테스트로 확실하니, 비지니스 로직을 확인)

    // 3. Controller 에서 (프론트 - 백) api 결과 반환을 확인 (프론트와 잘 연동 되는지를 확인)
}
